name: vyaparsense

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vyaparsense
      POSTGRES_PASSWORD: vyaparsense
      POSTGRES_DB: vyaparsense
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vyaparsense -d vyaparsense"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
  redisdata:
