# Memory — current working state

> Living scratchpad of where the project actually is. Update at the end of each work session. Newest at top.

## Snapshot (2026-06-02)
- **Phase:** Phase 0-5 ✅ COMPLETE (full ML→replenishment pipeline in `packages/ml`); API integration: forecast/accuracy/reorder/KPI endpoints (#55/#57/#59) + as-of cutoff & horizon-week accuracy (#69); only the ADR-007 async worker left. **Phase 6 (frontend) ✅ COMPLETE** — MVP product surface (plan.md §3) fully reachable in the UI (6 routes). **Phase 7 (multi-tenant SaaS / custom auth) IN PROGRESS** — primitives (#73), signup/login (#75), refresh rotation+reuse+`/me`+access dep (#77), tenant isolation (#79), and **web auth flow (#81: login/signup page, in-memory access token + auto-refresh on 401, route guard, all calls authenticated) — the app works end-to-end again with real auth.** Rate limiting on login/signup shipped (#93, per-IP fixed-window). Remaining: CSRF + Postgres RLS + email verify/reset (security-sensitive, full review before launch). **Async forecast jobs (ADR-007) ✅ COMPLETE end-to-end:** job model + `POST /forecast-jobs`/`GET /forecast-jobs/{id}` (#83); Redis-backed RQ worker with inline fallback (#85, ADR-011); jobs run the classical + intermittent ladder (#87) AND a global LightGBM that competes with the per-series champions by pooled-WAPE backtest (#89, rung 4); the web forecasts page enqueues a job, polls status (queued→running→done), then reads forecasts (#91) — the full ML ladder now reaches users with live progress. **Phase 8 (moat) STARTED** — hierarchical reconciliation (rung 5, #95) + transfer-learning cold-start (rung 6, #97 — honest backtest: loses to flat-mean on the intermittent sample, kept as a backtest-gated primitive). The full ML ladder (rungs 1-6) now exists in `packages/ml`. All code verified on `main`.
- **Repo:** github.com/shre111/VyaparSense. `main` protected (3 CI checks + up-to-date branch; no force-push/delete). main push-CI green. Only `main` branch remains on remote.
- **End-to-end pipeline (verified green on main):** CSV → `read_sales_csv` → `clean_sales` (dedupe + calendar gap-fill) → `to_series` → `classify_series` (ADI/CV²) → persisted per-tenant via `POST /tenants/{id}/uploads`. Forecasting: `select_per_series` runs an expanding-window backtest of all candidate models (baselines + classical + intermittent) and picks the lowest pooled-WAPE per series; probabilistic forecasts via `EmpiricalQuantileForecaster` + `quantile_backtest` (pinball/coverage). Global-model feature frame via `build_features` (calendar/price/promo/lags/rolling, leakage-safe); global `GlobalLightGBM` (recursive multi-step) scored by `global_backtest`; per-run reproducibility via `ModelCard` → `docs/model-cards/`. Replenishment math in `replenishment/` (safety stock, reorder point, EOQ, days-of-cover) + service-level reorder suggestions (`suggest_reorder` ties demand quantiles to reorder qty) + `simulate_policy` (day-by-day (s,S) sim → business KPIs). API: `POST/GET /tenants/{id}/forecasts` generate (baselines, sync) + read forecasts (append-only); `GET /tenants/{id}/accuracy` rolling WAPE per forecast-run week (the "getting smarter" chart data); `GET /tenants/{id}/reorder-suggestions` + `/simulation-kpis` (replenishment engine, before/after KPIs). Frontend: Next.js 15 App Router in `apps/web` — landing + upload + forecast chart (Recharts) + reorder-suggestions table; routes /upload /forecasts /reorder /accuracy (rolling-WAPE hero chart + before/after KPI cards). `POST /forecasts` accepts `as_of` (past cutoff → forecasts land on dates with realised actuals); `/accuracy` buckets rolling WAPE by horizon week. Auth in `app/security.py` (Argon2id + JWT) + `User`/`RefreshToken` models + `/auth/signup`,`/login`,`/refresh`,`/me`; business endpoints (uploads/forecasts/reorder/accuracy/kpis) now require a Bearer token and derive tenant from it (no `/tenants/{id}` in URLs) via `app/deps.py` `CurrentTenant`. Web auth: `AuthProvider`/`useAuth` + `/login` page + `RequireAuth` guard; `api.ts` holds the access token in memory and auto-refreshes on 401. Async forecast jobs (ADR-007): `forecast_jobs` table + `app/jobs.py` `run_forecast_job` (own session via `get_session_factory`/`SessionFactoryDep`); `POST /forecast-jobs` (202, queued) + `GET /forecast-jobs/{id}` poll; transport via `forecast_queue` setting — `inline` (`BackgroundTasks`, default, dev/CI) or `redis` (RQ worker, `app/task_queue.py`; ADR-011). Jobs run `generate_forecasts_full` (#89): per-series ladder (`FULL_LADDER_MODELS`: baselines + AutoETS/AutoARIMA + Croston/SBA/TSB) AND a global `GlobalLightGBM`, keeping whichever wins on pooled-WAPE (`repository.load_records` provides `SalesRecord`s w/ price/promo; returns a `LadderDecision` with both WAPEs); sync `POST /forecasts` stays baselines-only. Web forecasts page runs forecasting as a polled job (#91): `createForecastJob`→`pollForecastJob`(queued→running→done)→`getForecasts`, full ladder with live status. Login/signup rate-limited per IP (`app/ratelimit.py` fixed-window, in-memory; #93). Hierarchical reconciliation in `forecasting/hierarchy.py` (#95): `build_hierarchy` (total→store→SKU + summing matrix S), `aggregate_history`, `reconcile` (bottom_up/ols, bottom clamped ≥0 → coherent+non-neg), `coherence_error`. Cold-start in `forecasting/cold_start.py` (#97): `weekly_profile`/`pooled_weekly_profile` (demand-by-weekday, mean-1), `select_donors` (same-store first, else nearest by level), `cold_start_forecast` (donor shape × target level). Both are library primitives, NOT wired into the API/job path. **ML lib 240 tests, API 91 tests; web CI job active (build only — no web unit tests yet)** — all green in CI.
- **Tooling pinned (PR #30):** `ruff==0.15.15`, `mypy==1.13.0` in both pyproject dev extras, so local == CI. Root-cause fix for Phase 1's green-local/red-CI thrash (unpinned tools let CI pull newer ruff that flagged B008 `Depends()`-in-defaults; API uses `SessionDep = Annotated[...]`). API landed cleanly in PR #31.
- **GitHub identity:** push/PRs via `gh` as **shre111** (gh keyring auth, token scopes incl. repo/workflow). Repo-local git author = `shre111 <155060758+shre111@users.noreply.github.com>` to match existing history. Never push to `main` directly — feature branch + PR + squash-merge.

## Shipped (Phase 0 + 1)
- PR #6 — Phase 0 scaffold (monorepo dirs, gitignore/attributes/editorconfig, CONTRIBUTING, issue/PR templates, commitlint, pre-commit, Makefile, CI, docs/backlog.md).
- PR #8 — synthetic dataset `data/samples/sales_history.csv` (2 stores × 8 SKUs × 730 days = 11,680 rows) + seeded stdlib generator `scripts/generate_sample_sales.py`. 8/8 SKUs hit intended ADI/CV² quadrant.
- PR #10 — `packages/ml` python project (`vyaparsense_ml`, src layout, py3.11–3.13), ruff + mypy + pytest in CI.
- PR #13 — `schema.py`: `SalesRecord` (pydantic v2, frozen, extra=forbid), `CANONICAL_COLUMNS`, `DemandPattern`, `validate_rows()`.
- PR #16 — `ingest.py`: `read_sales_csv()` + `IngestError` (file/header checks, BOM-tolerant).
- PR #18 — `cleaning.py`: `clean_sales()` (dedupe, calendar gap-fill, price carry-forward), `to_series()`. Idempotent.
- PR #20 — `classification.py`: `classify_demand()`/`classify_series()` → `DemandStats` (ADI/CV², Syntetos-Boylan). Reproduces 8/8 sample quadrants end-to-end.
- PR #22 — `infra/docker-compose.yml` (postgres:16 + redis:7) + `.env.example`.
- PR #24 — `apps/api`: FastAPI + SQLAlchemy 2.0 + Alembic. Tables tenants/uploads/sales_records/forecasts (all `tenant_id`; forecasts append-only per ADR-008). Endpoints `/health`, `POST/GET /tenants/{id}/uploads`. SQLite+TestClient tests; Postgres/Alembic = prod source of truth. CI job `Python (ml + api)` lints/types/tests both packages.
- Docs PRs #11, #14, #25, #26, #27, #32 — memory/progress updates.

## Shipped (Phase 2)
- PR #33 — `vyaparsense_ml.forecasting` subpackage (pure stdlib, no new deps):
  - `models.py` — `Naive`, `MovingAverage`, `SeasonalNaive` behind a `Baseline` protocol.
  - `metrics.py` — `wape` (primary), `mase`, `rmse`, `bias`, `mape` (display-only, skips zero actuals) + `ForecastMetrics`/`compute_metrics`. Error ratios are fractions; display layer ×100. Per ADR-005.
  - `backtest.py` — expanding-window rolling-origin harness; pools every fold into one WAPE (the flywheel metric).
  - `selection.py` — `select_model`/`select_per_series` pick lowest pooled-WAPE model per series.
  - +42 tests (47→89). **Honest sample-data backtest** (16 series, 52 folds/series, h=7, step=7, 365-day warmup): `moving_average_7` wins 14/16; `naive` wins 1 (SKU-BATTERY-AA @ BLR), `seasonal_naive_7` wins 1 (SKU-GIFT-BOX @ BLR). Lumpy/intermittent SKUs (GIFT-BOX, PRESSURE-CK, BATTERY-AA, SHAMPOO-S) sit at WAPE > 1.0 → the gap Phase 3 targets.

## Shipped (Phase 3 ✅)
- PR #36 — `forecasting/classical.py`: `AutoETS` and `AutoARIMA` thin adapters over Nixtla `statsforecast` (ADR-004), conforming to the `Baseline` protocol so the Phase 2 harness drives them unchanged. First heavy deps added to `packages/ml` (`statsforecast>=2.0`, `numpy>=1.26`). Guards: require ≥2*season_length obs; clamp negative outputs to 0; fall back to seasonal-naive on non-finite optimizer output. +12 tests (89→101). **Backtest vs baselines** (same protocol): a classical model beats the best Phase 2 baseline on **14/16** series (ARIMA wins 10, ETS 4, naive 1, MA7 1). The 2 losses are the intermittent SKUs SKU-BATTERY-AA & SKU-SHAMPOO-S → exactly what Croston/SBA/TSB (next item) targets.
  - Process note: first PR (#35) failed commitlint `subject-case` (had `AutoETS`/`AutoARIMA` capitalized — subjects must be lower-case); closed and re-landed as #36 with `add auto-ets and auto-arima models`.
- PR #38 — `forecasting/intermittent.py`: `Croston`, `CrostonSBA`, `TSB` thin adapters over `statsforecast`, same `Baseline`-protocol pattern; flat forecasts. Guards: reject empty history/bad horizon/out-of-range alpha; clamp negatives; fall back to flat `Naive` on non-finite. +23 tests (101→124). **Backtest vs full 8-model pool** (same protocol): SBA wins 5/16; ARIMA still 8; naive/MA7/ETS 1 each; **Croston & TSB win nothing**. SBA modestly lowers best WAPE on the hardest SKUs vs #36 (GIFT-BOX@BLR 1.719→1.663, BATTERY-AA@DEL 1.426→1.424, SHAMPOO-S@DEL 1.197→1.196) but does NOT sweep the lumpy SKUs — ARIMA keeps PRESSURE-CK (both) & GIFT-BOX@DEL. Honest outcome: SBA earns a pool slot; Croston/TSB retained as benchmarks only.
- PR #40 — `forecasting/quantile_metrics.py`: `pinball_loss`/`mean_pinball_loss` (selection metric for probabilistic forecasts; pinball@0.5 = ½·MAE) + `coverage` (calibration diagnostic). Pure stdlib, +11 tests (124→135).
- PR #41 — `forecasting/quantile.py` + `quantile_backtest.py`: `QuantileForecaster` protocol + `EmpiricalQuantileForecaster` (wraps ANY `Baseline`; quantile = point + empirical-quantile of in-sample one-step residuals, clamped ≥0; model-agnostic/conformal — chosen because statsforecast interval support is inconsistent: ETS/ARIMA take `level=`, Croston family raises without conformal `prediction_intervals`). `quantile_backtest` mirrors the point harness, scored with pinball + coverage. +19 tests (135→154). **Calibration backtest** (Empirical[MA7], q={.5,.9,.95}, same protocol): mean coverage cov@.90=0.90, cov@.95=0.94 (well-calibrated where safety stock needs it); cov@.50=0.64 over-covers — entirely the intermittent/lumpy SKUs (zeros dominate → empirical residual median ≥0); smooth SKUs land on 0.50. Expected, reported honestly.

## Shipped (Phase 4 ✅)
- PR #43 — `forecasting/features.py`: `build_features(records, *, lags=(1,7,14), roll_windows=(7,28), dropna=True)` → pandas DataFrame, one row per (store,sku,date), for the global model. Calendar (dow/is_weekend/month/dayofyear/weekofyear + cyclical sin/cos), price/promo passthrough, lags, leakage-safe rolling mean/std (`groupby.transform(s.shift(1).rolling(w))` — window ends at t-1, never crosses series boundary; both asserted in tests). Declared `pandas>=2.0`. +9 tests (154→163). Sanity build on sample: 11232 rows × 22 cols, 16 series, 0 NaN after dropna (first 28 days dropped for roll_28 warmup).
- PR #45 — `forecasting/global_model.py` `GlobalLightGBM` (one booster across all series, M5 pattern; `regression_l1`, seeded) + `forecasting/global_backtest.py` `global_backtest` (rolling-origin over shared cutoff dates, fit-once-per-fold, pooled WAPE comparable to per-series). Recursive multi-step (predict→append→rebuild features via same `build_features`→repeat; clamp ≥0). Refactored `features.py` to expose `feature_columns()`/`CALENDAR_FEATURES` (single source of truth). Declared `lightgbm>=4.0` (4.6.0 has 3.13 wheel; deterministic w/ seed). +10 tests (163→173). **Honest backtest (apples-to-apples, 4 weekly folds, ~1.5y warmup): GLOBAL WINS — pooled WAPE 0.4129 vs per-series best-of-8 champions 0.4242 (~2.7% better), bias ≈ -0.2.** One global model beats the cherry-picked per-series ensemble → reproduces M5 on our data; rung earned. (Pooled MASE reads high — artifact of scaling against one series' naive across heterogeneous pool; WAPE is primary per ADR-005.) Note: `GlobalLightGBM` is NOT a per-series `Baseline` (fits across series), so it has its own fit/forecast API + backtest, not wired into `select_per_series` yet.
- PR #47 — `forecasting/model_card.py`: `data_hash()` (order-independent SHA-256 over canonical records), `ModelCard` dataclass + `to_markdown()`/`write_card()`, and `card_from_global_backtest()` in global_backtest.py (made `GlobalLightGBM.resolved_params()` public). Records data hash + features + params + metrics + backtest setup + lib versions per CLAUDE.md §7. +8 tests (173→181). Committed a real card `docs/model-cards/20260602T070632Z__global_lightgbm__ac48e772.md` (WAPE 0.4129, 11680 rows, deterministic).

## Shipped (Phase 5 ✅)
- PR #49 — new `replenishment/` subpackage (`inventory.py`): `service_level_z` (NormalDist.inv_cdf; 0.95→1.645), `safety_stock` (z·σ·√L), `reorder_point` (lead-time demand + safety stock), `economic_order_quantity` (√(2DS/H)), `days_of_cover` (inf if no demand, 0 if no stock), `order_up_to_level` (periodic-review, lead+review window). Pure stdlib (math + statistics). +19 tests (181→200) with hand-computed known values (safety stock 32.897, EOQ 223.607) + edge cases + validation. Deterministic formulas → exact-value tests, no backtest. Demand mean/std inputs come from the forecasting layer.
- PR #51 — `replenishment/suggestions.py`: `ReorderSuggestion` dataclass + `build_reorder_suggestion` (pure core: reorder point = Σ service-level-quantile demand over protection window lead+review; safety stock = ROP − median demand ≥0; order up to ROP, floored at EOQ) + `suggest_reorder(model, history, ...)` (wraps any `Baseline` in `EmpiricalQuantileForecaster` q={0.5, SL}, forecasts the window, builds the suggestion). **This is the payoff of the Phase 3 quantile work** — SL maps to a demand quantile, no normality assumption. Documented quantile-sum approximation (sum of daily quantiles ≠ quantile of total unless perfectly correlated; standard conservative practice). +11 tests (200→211). Demo (MA7, SKU-MILK-1L@DEL, lead 7, on_hand 60): safety stock rises 69.5→160.6→236.3 as SL 0.80→0.95→0.99; expected LTD ~307.5 stable.
- PR #53 — `replenishment/simulation.py`: `simulate_policy` (continuous-review (s,S) day loop: receive arrivals → lost-sales demand → review position & reorder; supports order-up-to-S or fixed-Q) → `SimulationResult` KPIs (fill rate, stockout rate/days, service level, units demanded/sold/lost, avg on-hand, dead-stock value, inventory turns, n_orders). Pure stdlib, deterministic. +10 tests (211→221), hand-traced known values. **Case-study demo (SKU-MILK-1L@DEL, 730d, lead 7): naive (no safety stock) vs forecast-driven (95% SS) → lost sales 2005→764 units (62% fewer), fill rate 0.944→0.979, stockout 0.059→0.022; trade-off avg on-hand 197→290, turns 170→120.** This is the before/after number for the GTM case study (CLAUDE.md §5 / §9).

## Shipped (API integration, in progress)
- PR #55 — wired `packages/ml` into `apps/api`. `app/forecasting.py` (`generate_forecasts`: per-series `select_per_series` over stored sales → forward forecast with the per-series baseline winner; series too short for a fold are skipped), repository helpers (`load_series` sales→to_series-shaped, `store_forecasts` append-only ADR-008, `list_forecasts`), `POST /tenants/{id}/forecasts?horizon=` + `GET /tenants/{id}/forecasts?store_id=&sku_id=`, schemas. **Baselines only in the request path (stdlib, sync); heavy classical/global models deferred to the async worker (ADR-007).** No migration needed (forecasts table shipped in #24). +8 tests (API 7→15). Before this, the API only did upload/clean/classify.
- PR #57 — `app/accuracy.py` (`accuracy_over_time`: bucket joined (created_at, predicted, actual) by ISO year-week of created_at → pooled `wape` from the ML lib, oldest first) + `repository.forecast_actual_pairs` (join `forecasts` point rows `quantile IS NULL` to `sales_records` on tenant/store/sku/horizon_date==date) + `GET /tenants/{id}/accuracy` → `[{period,n,wape}]` (inf WAPE → null for JSON). This is the **"getting smarter" hero-chart data**, reconstructable from the append-only log as actuals arrive. +8 tests (API 15→23).
- PR #59 — `app/replenishment.py` (`reorder_suggestions`: per-series `suggest_reorder` w/ MovingAverage(7) → `ReorderRow`; `policy_kpis`: per-series naive vs forecast-driven `simulate_policy`, pooled `KpiComparison`) + `routes/replenishment.py` `GET /tenants/{id}/reorder-suggestions` & `/simulation-kpis` (inventory params lead_time/service_level/on_hand/unit_cost as query params — NOT in DB yet, later schema change) + schemas. Series <14 days skipped. +9 tests (API 23→32).

## Shipped (Phase 6 — frontend ✅)
- PR #61 — `apps/web` Next.js 15 scaffold (App Router, React 19, TypeScript strict, Tailwind 3 + shadcn tokens/`cn` helper, lucide icons, eslint flat config). Landing page only (`src/app/page.tsx`: positioning + 3-step story). Hand-written (not create-next-app) for deterministic non-interactive setup; pinned deps + committed `package-lock.json`. **First activation of the CI `web` job** (was skipped; now `npm ci && lint && typecheck && build` on Node 22, green). Bumped next 15.1.6→15.5.19 to clear a critical CVE; 2 low/moderate transitive dev-tool advisories remain (postcss under next, @eslint/plugin-kit under eslint) — not fixable without `--force`/canary, left as-is. node_modules/.next gitignored. Local dev: `cd apps/web && npm install && npm run dev`.
- PR #63 — CSV upload page. `src/lib/api.ts` typed client (`uploadSalesCsv`→`UploadSummary`, `ApiError`; base URL `NEXT_PUBLIC_API_BASE_URL`, `.env.example`), shadcn-style `ui/button` + `ui/card`, `components/upload-form.tsx` (client; tenant+file inputs, idle/uploading/done/error, renders row/series counts + demand-pattern chips), `/upload` route + landing CTA. Sync upload (no job-status polling yet — that follows the async worker). No new deps (all primitives from scaffold lockfile). Build routes: `/`, `/upload`, `/_not-found`.
- PR #65 — per-SKU forecast chart. Added `recharts@2.15.4` (first charting dep). `api.ts` extended: `generateForecasts`/`getForecasts` + `ForecastRunSummary`/`ForecastItem`, refactored shared `requestJson`/`tenantPath`. `components/forecast-explorer.tsx` (client): run-forecast (POST then GET), store/SKU series picker, Recharts line of predicted units; shows chosen model. `/forecasts` route + CTAs. **Point line only — no confidence band** (forecasts endpoint is point-only; quantiles not exposed over API yet). Empty-state guidance for tenants with no forecastable series.
- PR #67 — reorder suggestions table. `api.ts`: `getReorderSuggestions(tenantId, {leadTimeDays,serviceLevel,onHand})` + `ReorderItem`/`ReorderParams`. `ui/table.tsx` primitives. `components/reorder-table.tsx` (client): policy-param form (lead time/service level/on-hand are API query params, not persisted) → table of reorder point/safety stock/order qty/days-of-cover (∞ when no demand) + reorder/ok badge. `/reorder` route + landing CTA. No new deps.
- PR #69 (API enabler for hero chart) — `POST /forecasts?as_of=YYYY-MM-DD` truncates each series to ≤cutoff and dates forecasts forward from it (so horizon dates fall on days with realised actuals; truncated series still need ≥35 pts = min_train 28 + horizon 7). `forecast_actual_pairs` + `accuracy_over_time` now key on **horizon_date** (not created_at), so a backfill across cutoffs spreads into a real weekly curve. Backward-compatible (no as_of = old behavior). +3 tests incl. end-to-end backfill→/accuracy non-empty (API 32→35).
- PR #71 — accuracy-over-time hero chart (the flagship "getting smarter" visual). `api.ts`: `getAccuracy`/`getSimulationKpis` + types, `generateForecasts(...,asOf?)`, `backfillAccuracy(tenantId,{lastSaleDate,weeks,horizon})` (POSTs forecasts at past weekly cutoffs so horizon dates overlap actuals). `components/accuracy-hero.tsx` (client): tenant + history-end inputs, "Build accuracy history" (backfill→reload) + "Load" actions, Recharts WAPE-% line (drops null-wape gaps), KPI cards (lost-sales reduction %, fill rate, units lost — forecast vs naive). `/accuracy` route + landing CTA. No new deps. **Completes the Phase 6 MVP surface (6 routes).**

## Shipped (Phase 7 — auth, in progress)
- PR #73 — auth security primitives (ADR-006), `apps/api/app/security.py`: `hash_password`/`verify_password` (Argon2id; verify returns False on wrong/malformed, never raises) / `needs_rehash`; `create_access_token`/`create_refresh_token` (JWT w/ sub/type/iat/exp/jti — jti is the hook for refresh reuse detection) / `decode_token(token, expected_type)` (validates sig+expiry+type → `TokenError`). Config: `auth_jwt_algorithm`=HS256, access TTL 15min, refresh TTL 30d; weak/default `auth_secret` (<32 chars) raises outside `development`. Deps `argon2-cffi>=23`, `pyjwt>=2.8` (3.13 wheels OK). +13 tests (API 35→48).
- PR #75 — `User` model (id, tenant_id FK, unique lower-cased email, Argon2id password_hash) + Alembic `0002_users` + `repository.get_user_by_email`/`create_user` + `routes/auth.py` `POST /auth/signup` (201, hash) + `/auth/login` (verify; rehash if needs_rehash). Refresh token → httpOnly + secure(non-dev) + SameSite=lax cookie scoped `/auth`; access token in body (`AuthResponse`). Uniform 401 (no user enumeration); case-insensitive unique email → 409; password ≥8. +11 tests (API 48→59).
- PR #79 — tenant isolation (security core of multi-tenancy). `app/deps.py` (new): `get_current_user` (moved out of routes/auth so business routers don't import a route module) + `CurrentUser`/`CurrentTenant` deps. **Breaking:** business routes drop the `/tenants/{id}` prefix (`POST /uploads`, `GET /forecasts`, `/accuracy`, `/reorder-suggestions`, `/simulation-kpis`, etc.) and require a Bearer access token; tenant derived from token. Closed a cross-tenant read leak (endpoints were previously unauthenticated). conftest `auth_client` fixture; all business tests updated to new URLs; `test_tenant_isolation.py` (acme can't see globex's data) + per-route 401 tests. +3 tests (API 68→71). **Web app is now broken until it authenticates — next PR.**
- PR #81 — web auth flow (fixes the #79 breakage). `api.ts` rewritten: in-memory access token, `authedFetch` (Bearer + `credentials:include`; on 401 → `POST /auth/refresh` once → replay), `login`/`signup`/`fetchCurrentUser`/`logout`; every business call dropped `tenantId` + uses new URLs. `components/auth-provider.tsx` (`AuthProvider`/`useAuth`, restores session via `/auth/me` on mount), `app/login/page.tsx` (login+signup toggle), `components/require-auth.tsx` (guard: spinner→redirect `/login`, header w/ user+logout). 4 feature pages wrapped in `RequireAuth`; their components lost the manual tenant input. 7 web routes; build green. No new deps. Access token is memory-only (lost on hard reload; restored via refresh cookie + `/auth/me`).
- PR #97 — transfer-learning cold-start (Phase 8 moat, rung 6). `forecasting/cold_start.py`: `weekly_profile` (demand-by-weekday norm to mean 1, None if all-zero), `pooled_weekly_profile` (avg donor shape, flat if none), `select_donors` (same-store first else nearest by level; ≥min_history), `cold_start_forecast(target, donors, horizon, start_date=)` = donor shape × target level (donor level when empty), clamped ≥0. **HONEST backtest (CLAUDE.md §7): on the intermittent 16-SKU sample cold-start LOSES to flat-mean (0.695 vs 0.449 WAPE, 7-day prefix); reported truthfully, kept as a backtest-gated primitive, NOT a default — the global model #89 is the cross-series transfer that wins here.** Tried level-shrinkage toward donor pool → worse at short prefixes (heterogeneous levels = bad prior), dropped it. +9 tests (ML 231→240). Library primitive, not wired into API.
- PR #95 — hierarchical reconciliation (Phase 8 moat, rung 5). `forecasting/hierarchy.py`: `Hierarchy` (nodes + summing matrix `S`), `build_hierarchy(keys)` (total→store→(store,sku) — the tree the schema supports; no category field yet), `aggregate_history` (sum bottoms to each node, missing=0), `reconcile(base, method=bottom_up|ols)` (`yhat=S·G·yhat_base`; OLS `G=(SᵀS)⁻¹Sᵀ`; bottom clamped ≥0 before aggregating → coherent AND non-neg), `coherence_error` (diagnostic). From-scratch numpy (no hierarchicalforecast dep); MinT is the follow-up. Exported from `vyaparsense_ml.forecasting`. +10 tests (ML 221→231). Library primitive — wiring into the forecast job is a separate slice.
- PR #93 — auth rate limiting (Phase 7 hardening, slice 1). `app/ratelimit.py`: `FixedWindowLimiter` (per-key fixed window) + `rate_limit_auth` dep (keys on `request.client.host`) + `reset_rate_limits()`. Applied to `/auth/login` + `/auth/signup` via `dependencies=[Depends(rate_limit_auth)]`; over cap → 429 + Retry-After. `auth_rate_limit_per_minute=10` setting. conftest autouse `_reset_rate_limits` (per-process state). In-memory/single-process (Redis-backed = scale-up, like forecast_queue). +3 tests (API 88→91). NEXT auth: CSRF for refresh cookie, then RLS, email verify/reset, full review.
- PR #91 — web forecast job-status UX (ADR-007 final slice; complete end-to-end). `api.ts`: `ForecastJobStatus`/`ForecastJobState` types + `createForecastJob`/`getForecastJob`/`pollForecastJob` (polls to terminal w/ `onUpdate`, 408 on timeout). `forecast-explorer.tsx`: "Run forecast" enqueues `/forecast-jobs` → polls (button shows Queued…/Forecasting…/Loading results…) → reads `/forecasts`; failed job shows server error. Now shows the FULL ladder (was sync baselines). Sync `generateForecasts` client kept but unused by web. lint/typecheck/build green, no new deps.
- PR #89 — global LightGBM in the job ladder (ADR-007 rung 4). `repository.load_records` (tenant-scoped `SalesRecord`s w/ price/promo). `app/forecasting.py` `generate_forecasts_full(records,...) -> (rows, LadderDecision)`: runs per-series ladder + `global_backtest`, compares pooled WAPE (`_pooled_champion_wape` pools champion folds the same way), forecasts forward with the winner — global only if it STRICTLY beats champions (ties → champions). `_GLOBAL_MIN_TRAIN_DAYS=42` (features need >28-day warmup; below → global_wape None → champions). `models`/`n_folds` params let tests run a cheap baseline set fast. `app/jobs.py` calls it. Honest backtest on 90-day sample window (16 series): champions win 0.4261 vs global 0.4332 (mostly croston_sba) — system correctly keeps champions; ~74s → why it's async. +5 tests (API 83→88). NEXT (last ADR-007 slice): web job-status UX.
- PR #87 — classical + intermittent ladder in jobs (ADR-007 slice 3a). `app/forecasting.py` `FULL_LADDER_MODELS` = baselines + `AutoETS(7)`/`AutoARIMA(7)` + `Croston`/`CrostonSBA`/`TSB`; `app/jobs.py` runner passes `models=FULL_LADDER_MODELS` to the existing `generate_forecasts` (per-series winner by rolling backtest). Sync `POST /forecasts` unchanged (baselines). No new deps (statsforecast/lightgbm via vyaparsense-ml), no schema/contract change. +2 tests (API 81→83); suite ~15s (statsforecast backtests). Per-job selection still all-or-nothing (one bad series fails the job → caught as `failed`). NEXT: global LightGBM (rung 4) needs a `SalesRecord` loader (price/promo) + pooled per-series-vs-global compare.
- PR #85 — Redis-backed RQ worker for forecast jobs (ADR-011). `forecast_queue` setting: `inline` (default; `BackgroundTasks`, dev/CI, no Redis) or `redis` (enqueue to RQ). `app/task_queue.py`: `enqueue_forecast_job` (producer) + `run_forecast_job_task` (consumer entrypoint the `rq worker` runs → opens own `SessionLocal`, calls `app.jobs.run_forecast_job`) + `get_queue`. Route `POST /forecast-jobs` branches on the setting. Deps `rq>=1.16`+`redis>=5`. **RQ over Arq/Celery: sync codebase → plain sync enqueue (ADR-011); worker forks → Linux deploy, not native Windows; local dev uses inline.** Worker = host process (no Dockerfile; compose only runs pg+redis). infra/README documents `rq worker forecasts --url …`. +3 tests (API 78→81, queue stubbed — no live Redis). Next: heavy model ladder in jobs, then web job-status UX.
- PR #83 — async forecast jobs (ADR-007, first slice). `ForecastJob` model (`forecast_jobs`: status queued→running→completed|failed, horizon, as_of, counts, error, timestamps) + Alembic `0004_forecast_jobs`. `app/jobs.py` `run_forecast_job(session_factory, job_id)` — opens its OWN session (job outlives the request) via new `get_session_factory` dep + `SessionFactoryDep`; marks running, generates+stores forecasts, marks completed/failed (catches all exceptions → terminal state). `POST /forecast-jobs` (202, queued) + `GET /forecast-jobs/{id}` (tenant-scoped poll; 404 cross-tenant/unknown). Interim transport = FastAPI `BackgroundTasks` (no Redis yet; sync `POST /forecasts` untouched so web keeps working). conftest overrides `get_session_factory` → in-memory factory (shared StaticPool). +7 tests (API 71→78). Repo helpers: `create_forecast_job`/`get_forecast_job`(scoped)/`get_forecast_job_by_id`(runner)/`mark_running`/`complete`/`fail`.
- PR #77 — refresh rotation + reuse detection + current-user. `RefreshToken` model (user_id, unique jti, revoked) + Alembic `0003_refresh_tokens` + repo helpers (`record_refresh_jti`/`get_refresh_token`/`revoke_refresh_jti`/`revoke_all_user_refresh`/`get_user`). `POST /auth/refresh` reads httpOnly `vs_refresh` cookie → rotates (revoke presented jti, issue new pair); replayed/revoked or unknown-signed jti → revoke whole user family + 401. `get_current_user` Bearer dep + `GET /auth/me`. Token-type enforced (access≠refresh). signup/login now persist the issued jti. +9 tests (API 59→68). **Still missing before launch: CSRF, rate limiting, tenant-isolation/RLS, email verify/reset; full security review. Business endpoints still take tenant_id in path, not yet behind `get_current_user`.**

## Decided
- **Brand: VyaparSense** (vyaparsense.com). Repo codename `kirana-demand`. (ADR-001)
- **Auth: custom** — FastAPI Argon2id + JWT access + rotating refresh cookies. No third-party. (ADR-006)
- Stack: Next.js/TS + FastAPI + Postgres/Redis + (statsforecast, LightGBM).
- Monorepo `apps/web`,`apps/api`,`packages/ml` (ADR-002). Next.js never touches ML DB directly (ADR-003).
- ML ladder: naive → classical → intermittent → global LightGBM → hierarchical → transfer-learning → reorder opt.
- Metrics: WAPE primary, MASE for intermittent, MAPE display-only (ADR-005).
- Forecasts append-only for the "getting smarter" chart (ADR-008).
- Forecast-job queue: **RQ** over Redis (sync codebase fits sync enqueue), inline `BackgroundTasks` fallback for dev/CI (ADR-011).
- Commits: single-line Conventional Commits, NO body, NO Co-Authored-By (ADR-009).

## Open questions (user input)
- ADR-010 pricing tiers per market (later). License (user unsure; default proprietary). Wedge (default D2C-first, generic ingestion).

## Next actions (Phase 7 auth in progress)
Primitives (#73), signup/login (#75), refresh rotation+reuse+`/me`+access dep (#77), tenant isolation (#79), web auth flow (#81) done. **The app works end-to-end again with real auth.** Remaining auth hardening, in priority order:
1. `feat(api): rate limiting + CSRF (cookie auth)` — NEXT. `/auth/refresh` is a cookie POST → needs CSRF protection (e.g. double-submit token); rate-limit `/auth/login` + `/auth/signup` against brute force. Then Postgres RLS (defense-in-depth behind app-level tenant scoping) + email verify/reset. **All security-sensitive — full review before launch (ADR-006).**
- Async forecast jobs (ADR-007) — ✅ DONE end-to-end (#83 model+endpoints, #85 RQ worker, #87 classical+intermittent, #89 global LightGBM, #91 web job-status UX). Possible future adds (not blocking): persist `LadderDecision` on the job row + surface "which model won + WAPEs" in the UI; per-series resilience so one bad series doesn't fail the whole job; run the RQ worker in a real deploy.
- **Two open tracks now lead:** (1) **Auth hardening** (Phase 7) — rate limiting done (#93); NEXT `feat(api): CSRF for the refresh cookie` (double-submit token; `/auth/refresh` is a cookie POST) → Postgres RLS → email verify/reset → full security review (security-sensitive, gates launch). (2) **Phase 8 moat** — ML ladder rungs 1-6 now all built (hierarchical #95, cold-start #97). What's left here is wiring the primitives into the product (`feat(api): reconcile job forecasts across the hierarchy`; MinT optional) — lower priority than launch-readiness. **The ML is essentially feature-complete; the gap to a launchable product is auth hardening (CSRF→RLS→email→security review) + Phase 9 polish (landing/demo, observability, case study from #53 KPIs 62%) + deploy.**
- Carry-overs (small): forecast confidence band (needs a quantile read endpoint); persist per-SKU inventory params (lead time/on-hand) instead of query params; LICENSE file (decision pending — user, default proprietary).
- **Python dev gotcha:** CI `api`/`ml` jobs run `ruff check .` **AND `ruff format --check .`** then `mypy` + `pytest`. `ruff check` alone is NOT enough — run `ruff format --check` (or `ruff format`) locally too or CI red-on-format. Interpreter: `apps/api/.venv\Scripts\python.exe` (the bare `python` on PATH is the Windows Store stub — not usable).
- **Web dev gotcha:** CI `web` job runs `npm ci && lint && typecheck && build` on Node 22 (local is Node 20). `next lint` rewrites `next-env.d.ts` to reference `.next/types` (gitignored) — harmless, typecheck tolerates it. Verify the full sequence locally before pushing.

## Local dev quickstart
- ML (Linux/Mac): `cd packages/ml && python3.13 -m venv .venv && .venv/bin/pip install -e ".[dev]" && .venv/bin/pytest`
- API (Linux/Mac): `cd apps/api && python3.13 -m venv .venv && .venv/bin/pip install -e ../../packages/ml && .venv/bin/pip install -e ".[dev]" && .venv/bin/pytest`
- DB: `make up`; migrations `cd apps/api && alembic upgrade head`
- **Windows (this machine):** no system Python and no `py` launcher — build venvs with conda base 3.13 (`C:\Users\91890\miniconda3\python.exe`). Venv tools live in `.venv\Scripts\` (e.g. `packages\ml\.venv\Scripts\pytest.exe`), not `.venv/bin/`. Docker 27 + Node 20 present. `.venv`s are gitignored.

## Notes / gotchas (hard-learned)
- **One PR at a time.** Cut each branch from FRESH `origin/main`. Don't start the next branch before the previous PR has merged.
- **Verify the EXACT tree you push.** Build the venv deliberately, WAIT, confirm the tool exists. Run `ruff format .` (not just `--check`). Ensure `git status` is clean before pushing — uncommitted working-tree fixes = green local / red CI.
- **After `gh pr merge`, confirm it merged** (check the file is on `origin/main`). Never let docs claim completion before code is confirmed on main.
- **`git push` hangs on this machine** (times out, exit 124) because the SYSTEM gitconfig (`C:/Program Files/Git/etc/gitconfig`) sets `credential.helper=manager` (Git Credential Manager). Git runs `manager` FIRST for github; once its cached token expires it blocks on an interactive prompt that never appears. A repo-local host helper alone does NOT fix it — git tries `manager` before reaching it. **Fix (persisted in this clone's `.git/config`):** reset the helper list then add gh, both host-scoped:
  `git config --local --add credential.https://github.com.helper ""` then `... --add credential.https://github.com.helper "!gh auth git-credential"`. The empty value clears `manager`; gh (authenticated) then serves the token non-interactively. Verify with `git push --dry-run` (returns instantly). One-off alternative: `git -c credential.helper= -c "credential.helper=!gh auth git-credential" push ...`. Re-apply on fresh clones.
- **commitlint requires lower-case subjects** (`subject-case` = lower-case, `header-max-length` 72). Camel-case identifiers in the subject (e.g. `AutoETS`) fail CI even though Python/Web pass — write `auto-ets`/`auto-arima`. Other rules pass cleanly on the standard single-line format.
- **`gh pr merge` quoting on PowerShell:** pass `--subject "..."` only; do NOT pass `--body ""` (the empty arg gets dropped and gh errors "flag needs an argument: --body"). Squash merge needs no body.
- `python3` may be 3.14 (too new for ML wheels) — use 3.13. ML targets 3.11–3.13.
- MAPE misleads on intermittent demand; **keep naive baselines as the permanent benchmark** (never delete); select per-series **by backtest**, not by vibe — a naive/seasonal model winning is valid. M5 → LightGBM beats DL on this data shape.
- Concrete model instances collected in a list infer as `list[object]` unless annotated — annotate `list[Baseline]` (the Protocol is structural; the classes share no nominal base), or mypy(strict) errors at the call site.
